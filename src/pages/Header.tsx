import { Box } from '@mui/material';

const Header = (): JSX.Element => {
  return (
    <Box component="header" sx={{ backgroundColor: 'lightGrey' }}>
      <h1>Monsters</h1>
    </Box>
  );
};

export default Header;
